const wrapper = new tui.BoxRenderable($.renderer, {
  id: 'wrapper',
  width: '100%',
  height: '100%',
  justifyContent: 'center',
  alignItems: 'center',
});

$.renderer.root.add(wrapper);

wrapper;
